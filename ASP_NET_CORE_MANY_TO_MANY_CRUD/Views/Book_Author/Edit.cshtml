@model ASP_NET_CORE_MANY_TO_MANY_CRUD.Models.Book_Author

@{
    ViewData["Title"] = "Edit";
}

<h1>Create</h1>

<h4>Book_Author</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <input type="hidden" asp-for="Id"/>

            <div class="form-group">
                <label asp-for="Book.BookName" class="control-label"></label>
                <select asp-for="BookId" class="form-control" asp-items="@(new SelectList(ViewBag.BookList, "BookId", "BookName", "BookType"));">
                    @*Selected Item Select Problem
                    Selected Item Select Problem
                    Selected Item Select Problem
                    Selected Item Select Problem
                    Selected Item Select Problem
                    Selected Item Select Problem*@
                </select>

                
            </div>

            <div class="form-group">
                <label asp-for="Author.AuthorName" class="control-label"></label>
                <select asp-for="AuthorId" class="form-control" asp-items="@(new SelectList(ViewBag.AuthorList, "AuthorId", "AuthorName", "Mail"));">
                    @*Selected Item Select Problem
                    Selected Item Select Problem
                    Selected Item Select Problem
                    Selected Item Select Problem
                    Selected Item Select Problem
                    Selected Item Select Problem*@
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
